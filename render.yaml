databases:
  # PostgreSQL Database
  - name: corrigeaqui-db
    databaseName: corrigeaqui
    user: corrigeaqui
    plan: free

services:
  - type: web
    name: corrigeaqui-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    # Health check configuration
    healthCheckPath: /actuator/health
    healthCheckInterval: 30
    # Auto-deploy on git push
    autoDeploy: true
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: corrigeaqui-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: corrigeaqui-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: corrigeaqui-db
          property: database
      - key: SPRING_DATASOURCE_USERNAME
        fromDatabase:
          name: corrigeaqui-db
          property: user
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: corrigeaqui-db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRATION
        value: "86400000"
      - key: UPLOAD_PATH
        value: /app/uploads
      - key: ALLOWED_ORIGINS
        value: https://corrigeaqui-frontend.onrender.com
      - key: JAVA_OPTS
        value: "-Xmx450m -Xms256m"

  # Frontend Next.js
  - type: web
    name: corrigeaqui-frontend
    env: docker
    dockerfilePath: ./frontend/Dockerfile.prod
    dockerContext: ./frontend
    plan: free
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://corrigeaqui-backend.onrender.com
      - key: NODE_ENV
        value: production
